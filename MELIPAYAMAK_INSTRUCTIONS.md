# راهنمای اتصال به ملی پیامک (Meli Payamak) در Supabase

برای اینکه سیستم لاگین با رمز یکبار مصرف (OTP) از طریق ملی پیامک کار کند، باید مراحل زیر را انجام دهید:

## ۱. دیپلوی کردن تابع (Function)
کد لازم برای ارسال پیامک در مسیر `supabase/functions/send-sms/index.ts` قرار گرفته است. این تابع باید روی Supabase دیپلوی شود.

1.  ترمینال را باز کنید و دستور زیر را اجرا کنید (نیاز به Supabase CLI دارید):
    ```bash
    supabase functions deploy send-sms
    ```
2.  پس از دیپلوی موفق، آدرس تابع را کپی کنید (چیزی شبیه به: `https://<project-ref>.supabase.co/functions/v1/send-sms`).

## ۲. تنظیم هوک (Hook) در داشبورد Supabase
برای اینکه Supabase هنگام درخواست ورود، به جای سرویس پیش‌فرض خود (Twilio) از تابع شما استفاده کند:

1.  به داشبورد Supabase و بخش **Authentication** بروید.
2.  از منوی سمت چپ، **Providers** را انتخاب کنید.
3.  گزینه **Phone** را پیدا کرده و تنظیمات آن را باز کنید.
4.  گزینه **SMS Provider** را فعال کنید.
5.  در بخش **SMS Hook** (یا Custom SMS Provider)، آدرس تابعی که در مرحله قبل کپی کردید را وارد کنید.
    *   **نکته:** متد باید روی `POST` باشد.

## ۳. تنظیم متغیرهای محیطی (Environment Variables)
برای اینکه تابع به پنل ملی پیامک دسترسی داشته باشد، باید نام کاربری و رمز عبور را ست کنید.

1.  در ترمینال دستور زیر را بزنید:
    ```bash
    supabase secrets set MELIPAYAMAK_USERNAME="نام_کاربری_شما"
    supabase secrets set MELIPAYAMAK_PASSWORD="رمز_عبور_شما"
    supabase secrets set MELIPAYAMAK_BODY_ID="شناسه_الگو" 
    ```
    *   **MELIPAYAMAK_BODY_ID:** اگر از سرویس اشتراکی (ارسال سریع با پترن) استفاده می‌کنید، شناسه الگو را اینجا وارد کنید. در غیر این صورت این متغیر را خالی بگذارید.

## ۴. اعمال مایگریشن پروفایل‌ها
برای اینکه کاربران جدید پس از اولین ورود به طور خودکار در جدول `profiles` ثبت شوند، فایل مایگریشن `005_create_profiles_trigger.sql` ایجاد شده است.
دستور زیر را برای اعمال آن اجرا کنید:
```bash
supabase db push
# یا اگر لوکال کار می‌کنید:
npm run apply:migration
```

## نحوه عملکرد سیستم
1.  کاربر شماره موبایل را در صفحه لاگین وارد می‌کند.
2.  برنامه `supabase.auth.signInWithOtp` را صدا می‌زند.
3.  Supabase یک کد ۶ رقمی تولید می‌کند و آن را به تابع `send-sms` می‌فرستد.
4.  تابع `send-sms` کد را از طریق ملی پیامک به کاربر ارسال می‌کند.
5.  کاربر کد را وارد می‌کند و اگر صحیح باشد:
    *   اگر کاربر قبلاً ثبت‌نام نکرده باشد، **به صورت خودکار در دیتابیس `auth.users` ساخته می‌شود**.
    *   تریگر دیتابیس (که در مرحله ۴ ساختیم) اجرا شده و یک سطر در `public.profiles` برای او می‌سازد.
    *   اگر کاربر قبلاً وجود داشته باشد، فقط لاگین انجام می‌شود و سشن (Session) فعال می‌شود.
