# Ad Category Classifier

سیستم تشخیص هوشمند دسته‌بندی آگهی‌ها بر اساس عنوان و توضیحات با استفاده از مدل BERT چندزبانه.

## ویژگی‌ها

- 🧠 **مدل هوشمند**: استفاده از BERT برای تشخیص دسته‌بندی
- 🌐 **چندزبانه**: پشتیبانی از زبان فارسی
- ⚡ **سریع**: پیش‌بینی در کمتر از 1 ثانیه
- 🔄 **Fallback**: در صورت خطا، از کلمات کلیدی استفاده می‌کند
- 📊 **اطمینان**: نمایش درصد اطمینان پیش‌بینی

## دسته‌بندی‌های پشتیبانی شده

- `shop_rent`: اجاره مغازه و غرفه
- `shop_sale`: فروش مغازه و غرفه
- `office_rent`: اجاره دفتر، اتاق اداری، مطب
- `industrial`: دفتر صنعتی، کشاورزی، تجاری
- `apartment_rent`: اجاره آپارتمان
- `apartment_sale`: فروش آپارتمان
- `villa_rent`: اجاره ویلا
- `villa_sale`: فروش ویلا
- `land`: زمین

## نصب و راه‌اندازی

### ۱. نصب وابستگی‌ها

```bash
pip install -r requirements.txt
```

### ۲. آموزش مدل

```bash
python train_ad_classifier.py
```

این دستور:
- مدل BERT چندزبانه را بارگذاری می‌کند
- با داده‌های نمونه آموزش می‌دهد
- مدل را در پوشه `./ad_classifier` ذخیره می‌کند

### ۳. تست مدل

```bash
python predict_category.py
```

### ۴. راه‌اندازی API

```bash
cd api
python main.py
```

API روی `http://localhost:8000` در دسترس خواهد بود.

## استفاده

### از طریق Python

```python
from predict_category import AdClassifier

# بارگذاری مدل
classifier = AdClassifier()

# پیش‌بینی
result = classifier.predict(
    title="اجاره مغازه در مرکز شهر",
    description="مغازه 50 متری در مرکز خرید"
)

print(f"دسته‌بندی: {result['category_name']}")
print(f"اطمینان: {result['confidence']:.2f}")
```

### از طریق API

```bash
curl -X POST "http://localhost:8000/predict-category" \
     -H "Content-Type: application/json" \
     -d '{
       "title": "اجاره مغازه در مرکز شهر",
       "description": "مغازه 50 متری در مرکز خرید"
     }'
```

### در React

```javascript
const predictCategory = async (title, description) => {
  const response = await fetch('/api/predict-category', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ title, description })
  });
  
  const result = await response.json();
  return result.category;
};
```

## ساختار پروژه

```
├── train_ad_classifier.py    # آموزش مدل
├── predict_category.py       # کلاس پیش‌بینی
├── requirements.txt          # وابستگی‌ها
├── api/
│   └── main.py              # FastAPI endpoint
└── ad_classifier/           # مدل آموزش داده شده
    ├── config.json
    ├── pytorch_model.bin
    └── tokenizer.json
```

## بهبود مدل

برای بهبود دقت مدل:

1. **افزودن داده‌های بیشتر**: نمونه‌های واقعی آگهی‌ها را به `training_data` اضافه کنید
2. **تنظیم hyperparameters**: `learning_rate`، `num_train_epochs` و `batch_size` را تنظیم کنید
3. **استفاده از مدل بهتر**: مدل‌های فارسی بهتر مانند `parsbert` استفاده کنید

## API Endpoints

- `POST /predict-category`: پیش‌بینی دسته‌بندی
- `GET /health`: بررسی وضعیت API
- `GET /categories`: لیست دسته‌بندی‌ها

## مثال خروجی

```json
{
  "category": "shop_rent",
  "category_name": "اجاره مغازه و غرفه",
  "confidence": 0.85,
  "all_predictions": {
    "shop_rent": 0.85,
    "shop_sale": 0.10,
    "office_rent": 0.03,
    "industrial": 0.01,
    "apartment_rent": 0.005,
    "apartment_sale": 0.003,
    "villa_rent": 0.001,
    "villa_sale": 0.001,
    "land": 0.000
  }
}
```

## نکات مهم

- مدل نیاز به GPU برای آموزش سریع‌تر دارد (اختیاری)
- برای تولید، از مدل‌های بزرگ‌تر و داده‌های بیشتر استفاده کنید
- API را پشت reverse proxy قرار دهید
- برای امنیت بیشتر، authentication اضافه کنید
